{
  "private": true,
  "scripts": {
    "prepublish": "typings install",
    "test": "typings bundle . -o bundle && ts-node test.ts"
  },
  "devDependencies": {
    "koa-compose": "^4.0.0",
    "ts-node": "^3.0.3",
    "typescript": "^2.3.2",
    "typings": "^2.1.1"
  }
}
